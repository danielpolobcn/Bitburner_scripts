// TARGET SERVER MUST BE ENTERED AS AN ARGUMENT, i.e. "run inspector.script n00dles"
var target = args[0];

var MaxMoney = getServerMaxMoney(target);
var CurrentMoney = getServerMoneyAvailable(target);
var MinSecurity = getServerMinSecurityLevel(target);
var CurrentSecurity = Math.floor(getServerSecurityLevel(target));
var MaxRam = getServerMaxRam(target);
var Ports = getServerNumPortsRequired(target);
var Level = getServerRequiredHackingLevel(target);

tprint("Max Money = ", MaxMoney / 1000000, " M");
tprint("Current Money = ", CurrentMoney / 1000000, " M");
tprint("Min Security = ", MinSecurity);
tprint("Current Security = ", CurrentSecurity);
tprint("Server RAM = ", MaxRam, " Gb");
tprint("Ports required to nuke = ", Ports);
tprint("Hacking level required = ", Level);